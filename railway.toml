[build]
builder = "dockerfile"

[deploy]
startCommand = "python main.py"
healthCheckPath = "/api/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services.tikdl_miniapp.variables]]
name = "FLASK_ENV"
value = "production"
